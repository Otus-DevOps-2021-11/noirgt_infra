- name: Install all soft for application
  hosts: all
  tasks:
    - name: Install programms
      apt: 
        name: "{{ item }}"
        state: present 
      with_items:
        - apt-utils
        - ruby-full
        - ruby-bundler
        - build-essential
      become: true

